# TII 設計文書（第1段階：実装前設計）

Transition-Ignition Identifier / 遷移発火識別子

本文書は仕様要件25「第1段階」に対応する。ここで確定するのは次の6点のみであり、
「世界を完全にモデル化する」ことは目標にしない。

1. TIIコア
2. イベントモデル
3. 識別子生成方法
4. 監査履歴
5. 任意モジュール構造
6. データ移植方法

---

## 0. 位置付け（要件1）

TIIは **下位の識別・監査インフラ** である。

- 固定的対象への永久番号付与ではない。
- 状態・遷移・発火・アドレス・ドメイン・境界・系列・所有を世界の普遍的構成単位として
  宣言する存在論ではない。
- ある差異または記録対象について **追跡を開始** し、その後に採用された記述・判定・関係・
  変更・停止・訂正・分岐・統合・再分類等を、**消去せず監査可能な履歴** として保持する。
- 「遷移」「発火」はTIIが重点的に扱う作動記述だが、普遍的存在論的原理としてハードコードしない。
- Ziran Systemその他の上位研究アーキテクチャを置換せず、その上位に立たない。

TIIコアの語彙自体も最終的・不可逆的なものとして扱わない（要件28、最終設計原則）。

---

## 1. TIIコア（要件2）

TIIコアの必須項目は次の10点のみ。これ以外はコアの必須フィールドにしない。

| # | 項目 | 実装フィールド |
|---|------|----------------|
| 1 | 不透明なTII | `tii` |
| 2 | 記録イベント | 1イベント = 1 JSON行 |
| 3 | イベント間参照 | `supersedes`, `prev_event_for_target`, `content` 内の自由参照 |
| 4 | イベント発生／登録時刻 | `recorded_at`（主張時刻）, `ledger_written_at`（台帳書込時刻） |
| 5 | 記録主体または記録機構 | `recorder: { id, kind, ... }` |
| 6 | 記録内容 | `content`（任意構造） |
| 7 | 証拠または根拠への参照 | `basis: []`（自由参照の配列） |
| 8 | 内容検証値を付与可能な構造 | `content_verification: { algo, value }?`（任意） |
| 9 | 外部参照を付与可能な構造 | `external_refs: []`（任意） |
| 10 | 直前記録との監査的連結 | `prev_hash` + `hash`（台帳全体の連鎖） |

**コアの必須フィールドにしないもの**：状態・遷移・発火・アドレス・ドメイン・所有・系列・系譜。
これらは必要になったときだけ「記述モジュール」（§5）として適用する。

---

## 2. TIIそのもの（要件3, 4, 28）

- TIIは意味を持たない不透明識別子。
- 暫定構文：`tii:` + 小文字英数字12桁（Crockford風アルファベット、`i l o u` を除外）。
  例：`tii:7k3m9q2xh4rt`
- 文字列に埋め込まないもの：組織名・個人名・所有主体・発行年・場所・国・対象カテゴリー・
  論文種別・版番号・アドレス・ドメイン・理論名・発火状態・遷移状態。
- TIIの付与が最低限意味するのは **「この参照点から追跡を開始した」** ことだけ。
- 本質的同一性・所有・真正性・学術的正当性・永続性を保証しない。
- **根TII ≠ 一系列**。根TIIは追跡開始の参照点にすぎず、後続記録により
  同一系列判定 / 複数系列への分割 / 別TIIとの統合 / 系列所属への異議 / 系列概念自体の不採用
  が可能（要件4, 13）。
- TII文字列は変更しないが、「それが何を追跡していると解釈されているか」は
  `interpretation.revised` イベントで改訂可能。
- **本番発行前の絶対条件（要件28）が未確定のため、本実装が発行する全TIIは
  `identifier_status: "test"`（試験用識別子）** として扱う。文字列自体は昇格しても不変。

### 生成方式（暫定）

- CSPRNG（`crypto.randomBytes`）＋ 棄却サンプリングでモジュロ偏りを除去。
- 発行時に台帳全体と衝突検査。衝突時は最大1000回再試行。
- 名前空間・大文字小文字規則・許可文字・長さ・解決URL・失効／移管・仕様版管理は
  SPEC.md §「本番発行前の絶対条件」で PROVISIONAL として明示。

---

## 3. イベントモデル（要件5, 18）

TII履歴は **追記型イベント** として記録する。既存記録を直接上書き・消去しない。

### 訂正の方式

```
旧記録を保持  →  訂正イベントを追加（supersedes: 旧event_id）  →  現在有効と解釈される記録を再計算
```

### イベント構造

台帳書込時に付与：`event_id`, `seq`, `ledger_written_at`, `prev_hash`, `hash`,
`prev_event_for_target`。

呼出側が与える：`tii`, `event_type`, `recorder`, `content`, `basis?`,
`external_refs?`, `content_verification?`, `supersedes?`, `recorded_at?`。

### イベント種別（要件5, 7, 23）

`event_type` は **自由文字列**。既知型の登録簿（`src/labels.js`）は
UIとグルーピングのための **参考情報にすぎず、検証で閉じない**。
未知・将来追加される種別もそのまま保存・表示する。

「遷移として分類すること自体が不適切だった」等の訂正も、
単に新しいイベント（`act: "withdraw"` や `transition.reclassified`）として表現できる。

---

## 4. 監査履歴（要件10, 21）

- ブロックチェーン・NFT・トークン・暗号資産は成立条件にしない。
- 各イベントに `prev_hash`（直前イベントのhash）と `hash`（本イベントの内容ハッシュ）。
- `hash = SHA-256( prev_hash + canonicalJSON(event − hash) )`
- canonicalJSON = キー再帰ソートの決定的直列化。
- `verify()` が連鎖を再計算し、改変（内容書換・行削除・並替）を検出。
- 将来：電子署名・複数署名・公開鍵方式を `content` またはイベント拡張として追加可能。

---

## 5. 任意モジュール構造（要件6–13, 23）

モジュールは **ハードコードされた存在論ではなく、イベントに対する規約** である。
エンジンはモジュールを特権化しない。

共通形：`content` に次を置くだけ。

```jsonc
{
  "module": "state | transition | ignition | address | domain | relation |
             series | boundary | scale | timespan | external_identifier | ...(開いた集合)",
  "ref":    "<同一の被記述項目についての記録をまとめる下位ID>",
  "act":    "introduce | apply | hold | stop | replace | redefine | dispute | withdraw | ...(開いた集合)",
  "description": "...",
  "...": "モジュール固有の自由フィールド"
}
```

要件1が列挙する8つの作動（導入・適用・保留・停止・置換・再定義・異議・撤回）を
`act` の既定語彙とするが、固定列挙にしない。

### 投影（現在有効と解釈される記録の再計算）

`src/projection.js` が全イベントを走査し：

- `module + ref` でバケツ化。
- `supersedes` されたイベントは「現在の読み」から除外（履歴には残る）。
- 各バケツの「現在の読み」= `withdraw` / `stop` でない最新イベント。無ければ null。
- `dispute` があれば `disputed: true`。
- **固定された "現在状態" が常に存在すると仮定しない**（要件15, 24）。
  記録の無いモジュールは表示しない。

### 各モジュールの非存在論的性格

- **状態（§6, 要件6, 17）**：全TIIが離散状態へ分割可能とは仮定しない。
  「以前はA・Bを別状態、後に連続過程として再記述」も `redefine` / 新 `ref` で表現。
- **遷移（§7, 要件7）**：A→Bを最終的存在論的事実として固定しない。
  前後・派生・分岐・統合・置換・停止・再開・撤回・再接続・循環・不明・異議ありは
  `content.relation_kind` の候補値（開いた集合）。
- **発火（§8, 要件8）**：真偽値へ還元しない。未評価／発火記録あり／非発火記録あり／
  停止記録あり／異議あり等は **表示ラベル（`src/labels.js`）であり、基礎データモデルの
  存在論的状態集合ではない**。
- **アドレス／ドメイン（§9, 要件9）**：TIIコアの絶対的基礎にしない。
  1記録に複数アドレス可。唯一の正本を自動特権化しない。アドレス≠ドメイン。
  アドレス概念自体が適用不能な記録も許容（モジュールを使わないだけ）。
- **時間・尺度・境界（§10, 要件10, 18）**：必須の普遍的固定属性にしない。
  異なる時間区間・尺度・境界から別々の記述が成立してよい。境界変更履歴も保存。
- **関係（§12, 要件12）**：単一必須 `owner` 属性を設けない。
  管理・保存・アクセス・変更・複製・配布・維持・停止・削除・移管・署名・著作権保持・
  資金提供・公開・検証 等は候補値（開いた集合）。
  「所有」が必要なら複数関係から局所的に導出・記述するが、TII成立条件にしない。
- **系列・系譜（§13, 要件13）**：記録対象そのものではなく、複数記録間に付与される
  **判定の一種**。同系列／別系列／不明／異議／分割／統合／判定撤回 を記録可能。
  過去の系列判定を上書き削除しない。旧判定・新判定・理由・主体・日時を保持。
- **外部識別子（§14, 要件14）**：DOI/ARK/ISBN/ORCID/URL/IPFS CID 等をTIIの競合対象に
  しない。`external_refs` に複数登録可。TIIは外部識別子が無くても成立。
  停止・移転・失効・変更の履歴も記録可能。

---

## 6. データ移植方法（要件20, 27）

- 特定クラウド企業に依存しない。依存関係ゼロ（Node.js標準ライブラリのみ）。
- **正本は追記型 JSON Lines 台帳** `data/ledger.jsonl`（1行1イベント）。
- 完全書き出し：`JSON` / `JSON Lines` / `CSV`（イベント平坦化）。
- 全TII記録を静的ファイル群として再構築可能：`node bin/tii.js rebuild-static`。
- クラウド／ホスティング／ドメイン管理／DB製品が変わっても、
  `ledger.jsonl` があれば既発行TIIを変更せず再構築できる。
- 特定企業の非公開機能を再実装の必須にしない。

---

## 7. 証拠と判定の分離（要件18）

**非常に重要**。次の3層を分離する。

1. 記録された証拠（`basis`, `content_verification`, `external_refs`）
2. 記録主体による判定（`recorder` + `content`）
3. 現在のシステム表示（`projection.js` の出力）

システムは証拠の存在から「唯一の正しい存在論的結論」を自動生成しない。
`証拠A存在 → 主体Xが「発火」判定 → 主体Yが異議 → 後に判定撤回` の全履歴を保持する。

---

## 8. 複数記録主体（要件19）

- 初期版は管理者一名運用可。書込は `TII_ADMIN_TOKEN`（設定時）で保護。
- `recorder` は各イベントに記録。将来：複数発行／記録／署名主体・委任・失効・
  権限移管・異議主体・外部監査主体を `recorder` 構造の拡張と専用 `event_type`
  （`recorder.delegated` 等）で追加可能。
- 単一組織を永久的最終裁定主体としてデータモデルに埋め込まない。

---

## 9. 開発段階（要件25）

- 第1段階：本文書（設計のみ）。 ← 現在地
- 第2段階：最小実用版（TII発行・イベント追加・履歴表示・外部参照・内容ハッシュ・
  アドレス記録・構造化書き出し）。
- 第3段階：状態・遷移・発火・系列・関係を任意モジュールとして追加。
- 第4段階：破壊試験（`test/destruction.test.js`、要件26の25シナリオ）。
